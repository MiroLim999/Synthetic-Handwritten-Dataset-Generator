# Runtime dependencies for Python >=3.10.
# constraints.txt is the CI-tested version snapshot. It is intentionally not a
# cryptographic lock file; see README.md for the update/audit policy.
-c constraints.txt

pillow>=10.0,<13
numpy>=2.5.1,<3
tqdm>=4.65,<5
